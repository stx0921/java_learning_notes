# Day40：Redisson 可重入性与分布式锁源码分析

> 本文分析 Redisson 分布式锁的三大核心机制：**可重入性（Reentrant）**、**可重试（Retry）** 与 **超时释放 / 看门狗（Watchdog）**。

---

## 一、可重入性源码分析

### 1. 实现原理

Redisson 的锁在 Redis 中使用 **Hash 数据结构** 存储，一把锁对应一个 Hash：

- **key**：锁名（如 `lock:xxx`），代表这一把锁
- **field**：线程标识 `UUID:threadId`，标记这把锁当前被哪个线程持有，用于"锁是不是自己的"判断（可重入的前提）
- **value**：加锁次数（重入计数）

**计数规则**：
- 加一次锁：`value + 1`
- 释放一次锁（unlock）：`value - 1`
- 当计数减到 `0` 时，删除该锁（Hash 消失），锁即释放

### 2. 可重入锁的加锁流程

`tryLock` 最终通过一段 **Lua 脚本** 原子执行加锁逻辑：

1. 判断锁是否存在
   - 锁**不存在**：以线程标识 `UUID:threadId` 为 field 写入 `value = 1`，并设置锁有效期 → 加锁成功
   - 锁**存在**：继续判断锁的 field 是否是自己的线程标识
     - 是自己的：`value + 1`，并**重置锁有效期** → 加锁成功（可重入）

     > 疑问：这里加锁是"重置有效期"吗？
     > 回答：是。可重入加锁成功时，Lua 脚本会先执行 `hincrby` 把计数 +1，再执行 `pexpire` 把过期时间重置为新的有效期（未指定 leaseTime 时，重置为看门狗时间，默认 30s）。也就是说每次重入都会"续期"一次，保证嵌套的外层业务执行期间，锁不会因为过期被提前释放。

     - 不是自己的：获取锁失败，返回锁的剩余有效期（TTL），进入等待重试或超时失败

### 3. 释放锁流程

调用 `unlock` 释放时，Lua 脚本会：

1. 先判断锁的 field **是否是自己的线程标识**
   - 不是自己的（锁已过期被其他线程持有、或本线程根本没持有锁）：**不能释放**，Redisson 会抛出 `IllegalMonitorStateException`（attempt to unlock lock, not locked by current thread...）。锁被其他线程重新获取后，原线程无权释放别人的锁
2. 是自己的：执行 `value - 1`
   - 计数减后 **大于 0**：说明外层还有重入未释放，保留锁并**重置锁有效期**，外层业务可继续执行
   - 计数减后 **等于 0**：删除锁（`del`），并执行 **publish 公告（广播）**，向订阅了该锁频道的等待线程发送解锁信号，提醒它们可以重新竞争

> 注意：计数减 1 后仍大于 0 时"重置有效期"，是为了支撑外层重入继续持有锁；如果业务本身执行时间很长，靠的是看门狗续期，二者不要混淆。

---

## 二、可重试与超时释放源码分析

### 1. 可重试是如何实现的？

`tryLock` 可传入 `waitTime`（最长等待时间）、`leaseTime`（锁有效期）和时间单位。

当线程没有成功获取到锁时，会在 `waitTime` 内做如下尝试：

1. 先尝试获取一次锁
   - 成功：直接返回
   - 失败：进入下一步
2. 判断剩余等待时间是否 **大于 0**
   - 已超时：返回 false（放弃获取）
3. 通过 `subscribe`（订阅）该锁的发布订阅频道（`redisson_lock__channel:{锁名}`），**等待锁释放的信号**
4. 收到解锁广播信号后，被唤醒，再次判断**是否已经超时**
   - 未超时：重新执行"获取锁"逻辑（回到第 1 步循环）
   - 已超时：返回 false

这就实现了**可重试性**：在 `waitTime` 内不断"等待解锁信号 → 重试抢锁"，直到成功或超时。

> 注意：订阅的是 Redis 的发布/订阅（Pub/Sub）频道；持锁线程释放锁时 `publish` 的广播，正是唤醒这些订阅线程的信号。

### 2. 超时释放是如何解决的？

使用**看门狗（Watchdog）**机制刷新锁的过期时间：

- 当**没有指定 `leaseTime`** 时，Redisson 使用默认锁有效期：看门狗时间（`lockWatchdogTimeout`，默认 **30 秒**）
- 获取锁成功后，若未指定 leaseTime，会启动**到期续期任务**（`scheduleExpirationRenewal`）

#### 续期任务是怎么操作的？

通过后台 **task 任务**，把续期任务的 `delay`（执行间隔）设置为**看门狗时间的三分之一**（默认约 **10 秒**），在业务执行完毕前反复执行：

- 每约 10 秒把锁的过期时间**重置为 30 秒**
- 只要业务没结束，锁的过期时间就不断被续回 30 秒，从而解决"业务执行时间超过锁有效期、锁被提前释放"的问题

> 注意：业务正常结束时（`unlock` 成功释放锁），应当**停止看门狗**（`cancelExpirationRenewal`），否则看门狗会一直刷新锁的释放时间，锁永远无法正常过期释放（锁泄漏）。

### 3. 为什么要用看门狗机制来刷新锁的释放时间？

- **没有看门狗的后果**：Redis 中的锁到期后会被自动删除（超时释放）。如果业务执行时间超过锁的有效期，锁会被提前释放，其他线程就能获取同一把锁进入临界区，分布式锁形同虚设（引发超卖、重复下单等并发问题）。
- **看门狗的本质**：把"一次性固定过期"变成"动态续期租约"——只要当前线程还持有锁，就定时续期，保证"业务没执行完，锁一定不丢"。
- **为什么不让锁永久有效，而非要设默认有效期 + 看门狗**：防止客户端宕机/断网时锁永远无法释放（死锁）。先设一个默认有效期兜底，再配合看门狗续期，就能同时兼顾"业务执行期间锁不丢"和"客户端挂了锁能自动释放"。
- **显式指定 leaseTime 的例外**：当调用方自己传入 `leaseTime` 时，Redisson 认为调用方明确知道锁的最长持有时间，**不会启动看门狗**；到期后由 Redis 强制释放锁，即使业务没执行完也不续期（通常用于严格避免锁泄漏的场景）。
