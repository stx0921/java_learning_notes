# Week01 - 微服务入门：Nacos、Akka 与接口幂等

> 本周主线：**Nacos（服务注册 + 配置管理）** → **Akka Actor（异步消息驱动，用于异构系统对接）** → **Redis 接口幂等（AOP 无侵入实现）**。
> 贯穿本周的三个问题：服务之间怎么找到彼此、配置怎么统一管；两个系统各有一份数据时"谁说了算"；同一个请求重复发来，怎么保证只生效一次。

## 一、Nacos

**是什么？**

- **微服务注册中心**：服务启动后把自身注册到 Nacos，调用方先从 Nacos 拿到可用的服务实例地址，再发起调用，不必在代码里写死对方的 IP 与端口。
- **配置中心**：把配置文件统一放到 Nacos 管理，方便配置文件的管理与修改，不用到每个服务里逐个改、逐个重启。

**为什么需要它？**

微服务把系统拆成多个独立部署的服务，随之而来两个问题：

| 问题       | 没有 Nacos 时                    | 有了 Nacos 后               |
| -------- | ----------------------------- | ------------------------ |
| 服务地址是动态的 | 调用方写死 IP:PORT，实例扩容、下线、迁移都要跟着改 | 实例启动自动注册，调用方按服务名从注册中心取地址 |
| 配置文件分散   | 每个服务各留一份配置，改一个公共配置要全改一遍       | 配置集中存放、统一维护，还能按环境区分      |

## 二、Akka：基于 Actor 模型的异步框架

### 1. 是什么

Akka 是**基于 Actor 模型实现**的异步框架。

### 2. Actor 模型解决了什么痛点

传统并发编程的痛点是「**线程 + 共享内存 + 锁**」，Actor 模型对这一套做了两处替换：

| 传统并发 | Actor 模型 |
| --- | --- |
| 并发单元 = 线程 | 并发单元 = Actor |
| 共享内存 + 锁 | 消息传递代替锁的作用（数据进邮箱，Actor 串行取） |

### 3. 消息发送的两种方式：tell 与 ask

| 方式 | 特点 |
| --- | --- |
| `tell` | 异步操作，发完就走，不等待对方返回结果 |
| `ask` | 返回 `Future` 类型，可以异步拿到对方的响应结果 |

### 4. 我的理解

大概懂了这个 Akka，它并不是一个很复杂的东西。我的理解是它有点像异步操作：数据传进来若正常，直接返回 success，避免阻塞等待；真正的数据被投递到**邮箱**中，这个邮箱本质上就是一个消息队列，然后后台按照队列慢慢处理传进来的数据。

关键在于这个队列是由 Actor **串行**消费的（相当于单线程），同一时刻不会有两条消息被并发处理，也就避免了并发操作共享数据的问题——这正是"用消息传递代替锁"的含义。

所以使用 Akka 的目的很直接：**防止处理阻塞导致的访问超时**。

## 三、异构系统对接：两份数据到底谁说了算

**背景**

两个系统各有一份数据，两边数据不一致、字段各有取舍，属于**异构系统之间的数据对接**，各自的数据由各自的模块负责。

**为什么不能直接合并数据库？**

两边各自维护确实会造成频繁同步的麻烦，但合并数据库会引起很多问题，例如权限、查库速度等等。

**分库的好处**

- 口径一致
- 权责边界
- 独立演进

**工程化怎么解决"谁说了算"**

-> 通过 **Akka Actor 模型** 解决：外部数据先投递到邮箱，由 Actor 串行取出处理，再按类型分发到对应的 handler，由 handler 判断这条数据应该新增、修改还是忽略。两边数据各管各的，同步动作被收敛成一条串行的消息流，不会并发改动同一份数据。（Akka Actor 模型 -> 已解决）

## 四、基于 Redis 的接口幂等实现

### 1. 幂等是什么意思

同一个操作执行一次和执行多次，对系统造成的最终影响是一样的。数学中的概念就是 `f(f(x)) = f(x)`。

幂等是一个挺重要的功能，特别是在**支付、订单**这类不能重复扣款、重复下单的功能上，高并发场景下也经常用到。

### 2. 本项目怎么做的

- 使用 **Redis 做幂等组件**；
- 再利用 **AOP 切面**，把幂等组件无侵入地插入业务代码；
- 可以和 **Redis 分布式锁** 做比较。

### 3. 设计要点

| 要点 | 说明 |
| --- | --- |
| 切面注解 | 注解中记录 key、TTL、message 三个要素 |
| SETNX 做幂等标记 | 只有 key 不存在时才能设置成功，用"设置是否成功"判断是不是第一次请求 |
| TTL | 幂等标记的过期时间，避免标记长期占用 Redis |
| message | 重复请求时抛出的提示信息 |

**逻辑链路**

1. 第一次请求：创建 key，并设置对应 TTL；
2. 后续请求：发现 key 已被创建，throw message，请求不再执行业务；
3. 结果：多次请求只生效一次，达到"多次请求实现一次请求成功"的幂等效果。

### 4. 与 Redis 分布式锁的对比

| 维度 | 接口幂等（SETNX 标记） | Redis 分布式锁 |
| --- | --- | --- |
| 目的 | 同一请求多次执行只有一次生效 | 同一时刻只有一个线程进入临界区 |
| 生命周期 | 标记按 TTL 存活，过期后同一 key 还能再次进入 | 业务执行完立即释放（或靠看门狗续期） |
| 重复请求的处理 | 直接拒绝，抛出 message | 等待重试，拿到锁后照常执行业务 |
| 是否需要归属校验 | 不需要，谁先设置成功算谁 | 通常需要线程标识 + Lua 保证校验与释放原子性 |

## 本周总结

| 知识点 | 要点说明 |
| --- | --- |
| Nacos | 微服务的注册中心 + 配置中心，解决服务地址动态变化与配置分散的问题 |
| Actor 模型 | 并发单元从线程换成 Actor，共享内存 + 锁换成消息传递（邮箱） |
| tell / ask | tell 异步、不等待返回；ask 返回 Future，可以拿到结果 |
| 异构系统对接 | 两边各有一份数据、字段各有取舍时，通过 Actor 串行处理消息来决定数据的新增与修改 |
| 接口幂等 | 同一操作执行多次与执行一次影响相同，即 f(f(x)) = f(x) |
| Redis 幂等实现 | SETNX + TTL 做幂等标记，AOP 切面无侵入，重复请求抛 message |
| 对比分布式锁 | 幂等关注"重复请求不重复生效"，分布式锁关注"同一时刻互斥" |

## 待验证的问题

- Nacos 配置修改后，服务是怎么感知到并生效的？动态刷新是怎么实现的。
- Actor 的邮箱是串行消费，消息积压时怎么办？业务处理耗时过长会不会把队列拖垮。
- 幂等 key 怎么设计更合理（用户 + 业务 + 请求参数？），TTL 设多长比较合适。
- 幂等标记和分布式锁都是基于 SETNX，两者在项目里分别用在什么场景。
